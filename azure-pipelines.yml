# ASP.NET

# Build and test ASP.NET projects.
# Add steps that publish symbols, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/aspnet/build-aspnet-4

trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:PackageLocation="$(build.artifactStagingDirectory)"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
- task: Npm@1
  inputs:
    command: 'install'
    workingDir: 'project2-angular'
  displayName: 'Install Node.js'
  
- script: |
    cd project2-angular
    npm install -g @angular/cli@12.2.11
    npm install
    ng build
  displayName: 'npm install and build'

- task: AzureFileCopy@3
  inputs:
    SourcePath: "$(Pipeline.Workspace)/s/NET Project 2/project2-angular/dist/project2-angular"
    azureSubscription: 'Azure subscription 1(b8bc6e0a-4092-45fe-8320-2d2f94e8fd17)'
    Destination: 'AzureBlob'
    storage: 'emillerazuretraining'
    ContainerName: '$web'